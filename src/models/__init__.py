"""
src.models - Individual model implementations.

Each model file exposes a consistent interface:
    - train(X_train, y_train, **kwargs)  → fitted model
    - predict(model, X_test)             → predictions array
"""
